import React from 'react'
import { Box, styled } from '@mui/material'

const CustomBox = styled('Box')({
  display: 'flex',
  flexDirection: 'row',
  alignItems: 'center',
  marginTop: 8
})

export { CustomBox }
